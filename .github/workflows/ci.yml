name: Compiler CI

on:
  push:
    branches: [ main, dmm/* ]
  pull_request:
    branches: [ main ]

jobs:
  tests-ubuntu:
    runs-on: ubuntu-latest    
    steps: 
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Install Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential libreadline-dev ocl-icd-opencl-dev
      - name: List files and show Makefile
        run: |
          pwd
          ls -lR
          cat Makefile
      - name: Build cucpp
        run: |
          make re
      - name: Run Integration Tests
        run:
          make test

  tests-fedora-42:
    runs-on: ubuntu-latest
    container:
      image: fedora:42
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Install Dependencies
        run: |
          dnf -y update
          dnf -y install @development-tools readline-devel opencl-headers pocl-devel clang
      - name: Build cucpp
        run: |
          make re
      - name: Run Integration Tests
        run: |
          make test

    # alpine to test with musl
