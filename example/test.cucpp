compute z(a)
{
    a1 = a - 1
    return a1
}

compute loop(x)
{
    if (x > 0)
        return loop(x - 1)
    return x
}

compute main()
{
    x1 = 12
    x2 = loop(x1)
    x3 = x2 - 1
    a = z(x3)
    print(x1)
    return 0
}
